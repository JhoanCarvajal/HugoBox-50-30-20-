// Placeholder — se reemplaza en la Tarea 13
import { View, Text, StyleSheet } from 'react-native';

export default function Historial() {
  return (
    <View style={s.c}>
      <Text>Historial</Text>
    </View>
  );
}

const s = StyleSheet.create({
  c: { flex: 1, alignItems: 'center', justifyContent: 'center' },
});
