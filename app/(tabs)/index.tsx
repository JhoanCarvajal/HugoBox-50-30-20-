// Placeholder — se reemplaza en la Tarea 11
import { View, Text, StyleSheet } from 'react-native';

export default function Cajas() {
  return (
    <View style={s.c}>
      <Text>Cajas</Text>
    </View>
  );
}

const s = StyleSheet.create({
  c: { flex: 1, alignItems: 'center', justifyContent: 'center' },
});
