# HugoBox Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir HugoBox, una app móvil de presupuesto por "cajas" (reparto automático de ingresos por % y egresos por caja), con login Google, datos en Firestore y publicable en Play Store.

**Architecture:** App React Native (Expo, TypeScript) organizada por *features*. Las pantallas usan hooks de dominio que llaman a servicios/repositorios; los servicios son la única capa que habla con Firebase (Auth + Firestore). Sin API propia: la autorización vive en Security Rules. La lógica de dinero (reparto por %, redondeo, reversión de saldos) es pura y testeada con TDD; los servicios se testean contra el Firebase Emulator.

**Tech Stack:** Expo (managed) + expo-router, TypeScript, Firebase JS SDK (Auth + Firestore), `@react-native-google-signin/google-signin`, Zustand, React Hook Form + Zod, Jest + @testing-library/react-native, Firebase Emulator Suite, EAS Build/Submit.

## Global Constraints

- Lenguaje: **TypeScript** en todo el código de `app/` y `src/`.
- Nombre de la app: **HugoBox**.
- Moneda del MVP: una sola, campo `monedaPreferida` (default `"COP"`).
- Dinero en **centavos enteros** (ej. $100.01 = 10001). `monto` y `saldo` son enteros (centavos), nunca floats. La UI convierte: `÷100` al mostrar, `×100` (con `aCentavos`) al capturar. Σ(partes del reparto) = monto exacto, con aritmética entera (sin ruido de punto flotante).
- Cada usuario solo accede a `users/{uid}/**` (Security Rules).
- Las 3 cajas por defecto (Gastos 50 / Inversión 20 / Ahorro 30) no se eliminan en el MVP; sí se editan sus %.
- Los % solo afectan ingresos futuros; nunca redistribuyen saldos existentes.
- Egreso > saldo de la caja: se permite, devolviendo una bandera de advertencia (la UI avisa).
- Σ(porcentajes de cajas activas) debe ser 100 al crear/editar cajas.
- Las pantallas NUNCA llaman a Firestore directo: pasan por la capa de servicios.
- Auth del MVP: solo Google Sign-In (requiere development build, no Expo Go).
- Commits frecuentes, uno por tarea como mínimo.

---

## File Structure

```
app/                              # Expo Router
  _layout.tsx                     # root layout + guard de sesión
  (auth)/login.tsx                # pantalla de login (Google)
  (tabs)/_layout.tsx              # tabs: Cajas | Historial
  (tabs)/index.tsx                # dashboard de cajas
  (tabs)/historial.tsx            # historial + filtros
  transaccion/nueva.tsx           # registrar ingreso/egreso
  cajas/index.tsx                 # gestionar cajas (crear/editar %)
src/
  lib/firebase.ts                 # init Firebase (app, auth, db)
  lib/errors.ts                   # tipos de error y helpers
  types/models.ts                 # Caja, Transaccion, Reparto, Usuario
  utils/reparto.ts                # repartirIngreso() (lógica pura)
  utils/dinero.ts                 # redondear2, formatearMoneda
  features/cajas/cajasSchema.ts   # Zod: validación de cajas y %
  features/cajas/cajasService.ts  # CRUD cajas + cajas por defecto
  features/cajas/useCajas.ts      # hook onSnapshot de cajas
  features/transacciones/txSchema.ts
  features/transacciones/transaccionesService.ts
  features/transacciones/useTransacciones.ts
  features/auth/authService.ts    # Google Sign-In + crear usuario
  features/auth/useAuth.ts        # hook de sesión
  stores/sessionStore.ts          # Zustand: usuario actual
  components/ErrorBoundary.tsx
  components/CajaCard.tsx
firestore.rules                   # Security Rules
firebase.json                     # config emulador
app.json                          # config Expo (nombre, package id, plugins)
eas.json                          # perfiles de build
jest.config.js / jest.setup.ts
```

---

## Task 1: Scaffolding del proyecto Expo + TypeScript + Jest

**Files:**
- Create: proyecto Expo en la raíz, `jest.config.js`, `jest.setup.ts`, `src/utils/dinero.ts`, `src/utils/__tests__/dinero.test.ts`
- Modify: `package.json` (scripts y deps)

**Interfaces:**
- Produces: `redondear2(n: number): number`, `formatearMoneda(n: number, moneda?: string): string`

- [ ] **Step 1: Crear el proyecto Expo con TypeScript**

```bash
# En la raíz del proyecto (ya contiene docs/ y .git)
npx create-expo-app@latest . --template blank-typescript
```
Si pregunta por sobrescribir, conservar `docs/` y `.gitignore`. Aceptar la plantilla.

- [ ] **Step 2: Instalar dependencias**

```bash
npx expo install expo-router expo-dev-client react-native-safe-area-context react-native-screens @react-native-async-storage/async-storage
npm install firebase zustand react-hook-form zod @react-native-google-signin/google-signin
npm install -D jest jest-expo @testing-library/react-native @types/jest
```

- [ ] **Step 3: Configurar Jest**

`jest.config.js`:
```js
module.exports = {
  preset: 'jest-expo',
  setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'],
  transformIgnorePatterns: [
    'node_modules/(?!(jest-)?react-native|@react-native|expo(nent)?|@expo|expo-router|firebase|@firebase|@react-native-google-signin)',
  ],
};
```
`jest.setup.ts`:
```ts
// Espacio para mocks globales (se añaden en tareas posteriores).
```
En `package.json`, añadir a `"scripts"`: `"test": "jest"`, `"test:watch": "jest --watch"`.

- [ ] **Step 4: Escribir el primer test (de `dinero`)**

`src/utils/__tests__/dinero.test.ts`:
```ts
import { aCentavos, aUnidades, formatearMoneda } from '../dinero';

describe('aCentavos', () => {
  it('convierte unidades a centavos enteros', () => {
    expect(aCentavos(100.01)).toBe(10001);
    expect(aCentavos(33.33)).toBe(3333);
    expect(aCentavos(0.1)).toBe(10);
  });
});

describe('aUnidades', () => {
  it('convierte centavos a unidades', () => {
    expect(aUnidades(10001)).toBe(100.01);
  });
});

describe('formatearMoneda', () => {
  it('formatea centavos con símbolo y 2 decimales', () => {
    expect(formatearMoneda(123450)).toContain('1.234,5'); // 123450 centavos = 1.234,50
  });
});
```

- [ ] **Step 5: Ejecutar el test y verificar que falla**

Run: `npm test`
Expected: FAIL — `Cannot find module '../dinero'`.

- [ ] **Step 6: Implementar `dinero.ts`**

`src/utils/dinero.ts`:
```ts
// El dinero se representa SIEMPRE como centavos enteros. $100.01 = 10001.

/** Unidades monetarias (posible decimal del usuario) → centavos enteros. */
export function aCentavos(unidades: number): number {
  return Math.round(unidades * 100);
}

/** Centavos enteros → unidades monetarias. */
export function aUnidades(centavos: number): number {
  return centavos / 100;
}

/** Formatea un monto en centavos como moneda. */
export function formatearMoneda(centavos: number, moneda = 'COP'): string {
  return new Intl.NumberFormat('es-CO', {
    style: 'currency',
    currency: moneda,
    minimumFractionDigits: 2,
  }).format(centavos / 100);
}
```

- [ ] **Step 7: Ejecutar el test y verificar que pasa**

Run: `npm test`
Expected: PASS (2 suites). Si `Intl` no formatea en el entorno de test, ajustar el assert a `toContain('1.234')`.

- [ ] **Step 8: Verificar que la app arranca**

Run: `npx expo start` (abrir en Android). Expected: pantalla por defecto sin errores. Cerrar.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "chore: scaffolding Expo + TypeScript + Jest y utilidad de dinero"
```

---

## Task 2: Tipos del dominio

**Files:**
- Create: `src/types/models.ts`

**Interfaces:**
- Produces: `Caja`, `NuevaCaja`, `Transaccion`, `Reparto`, `Usuario`, `TipoMovimiento`

- [ ] **Step 1: Definir los tipos**

`src/types/models.ts`:
```ts
export type TipoMovimiento = 'ingreso' | 'egreso';

export interface Caja {
  id: string;
  nombre: string;
  porcentaje: number;   // 0..100; suma 100 entre cajas activas
  saldo: number;        // acumulado
  esPorDefecto: boolean;
  orden: number;
  createdAt: number;    // epoch ms
}

export type NuevaCaja = Pick<Caja, 'nombre' | 'porcentaje'>;

export interface Reparto {
  cajaId: string;
  monto: number;
}

export interface Transaccion {
  id: string;
  tipo: TipoMovimiento;
  monto: number;            // positivo
  fecha: number;            // epoch ms
  descripcion: string;
  cajaId: string | null;    // egreso → caja origen
  reparto: Reparto[];       // ingreso → cómo se dividió
  createdAt: number;
}

export interface Usuario {
  uid: string;
  email: string | null;
  displayName: string | null;
  monedaPreferida: string;  // "COP"
  createdAt: number;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/types/models.ts
git commit -m "feat: tipos del dominio (Caja, Transaccion, Usuario)"
```

---

## Task 3: Lógica pura de reparto de ingresos (TDD)

**Files:**
- Create: `src/utils/reparto.ts`, `src/utils/__tests__/reparto.test.ts`

**Interfaces:**
- Consumes: `Caja`, `Reparto` (Task 2), `redondear2` (Task 1)
- Produces: `repartirIngreso(monto: number, cajas: Pick<Caja,'id'|'porcentaje'>[]): Reparto[]`

- [ ] **Step 1: Escribir los tests**

`src/utils/__tests__/reparto.test.ts`:
```ts
import { repartirIngreso } from '../reparto';

const cajas = [
  { id: 'g', porcentaje: 50 },
  { id: 'i', porcentaje: 20 },
  { id: 'a', porcentaje: 30 },
];

describe('repartirIngreso (centavos enteros)', () => {
  it('reparte exacto 50/20/30 de 10000 centavos', () => {
    expect(repartirIngreso(10000, cajas)).toEqual([
      { cajaId: 'g', monto: 5000 },
      { cajaId: 'i', monto: 2000 },
      { cajaId: 'a', monto: 3000 },
    ]);
  });

  it('asigna el residuo entero a la caja de MAYOR porcentaje', () => {
    // 10001 entre 33/33/34: floor → 3300,3300,3400 (suma 10000); residuo 1 → caja de 34%
    const partes = repartirIngreso(10001, [
      { id: 'a', porcentaje: 33 },
      { id: 'b', porcentaje: 33 },
      { id: 'c', porcentaje: 34 },
    ]);
    expect(partes).toEqual([
      { cajaId: 'a', monto: 3300 },
      { cajaId: 'b', monto: 3300 },
      { cajaId: 'c', monto: 3401 },
    ]);
    expect(partes.reduce((s, p) => s + p.monto, 0)).toBe(10001);
  });

  it('la suma de partes SIEMPRE iguala el monto (barrido)', () => {
    for (let monto = 1; monto <= 5000; monto += 7) {
      const partes = repartirIngreso(monto, [
        { id: 'a', porcentaje: 33 },
        { id: 'b', porcentaje: 33 },
        { id: 'c', porcentaje: 34 },
      ]);
      expect(partes.reduce((s, p) => s + p.monto, 0)).toBe(monto);
    }
  });

  it('lanza si los porcentajes no suman 100', () => {
    expect(() => repartirIngreso(10000, [{ id: 'a', porcentaje: 90 }])).toThrow();
  });

  it('lanza si el monto no es un entero positivo', () => {
    expect(() => repartirIngreso(0, cajas)).toThrow();
    expect(() => repartirIngreso(100.5, cajas)).toThrow();
  });
});
```

- [ ] **Step 2: Ejecutar y verificar que falla**

Run: `npm test -- reparto`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementar `reparto.ts`**

`src/utils/reparto.ts`:
```ts
import { Caja, Reparto } from '../types/models';

type CajaReparto = Pick<Caja, 'id' | 'porcentaje'>;

/** Reparte un ingreso (en centavos enteros) entre cajas según su porcentaje.
 *  Garantiza Σ(partes) === montoCentavos exacto; el residuo va a la caja de mayor %. */
export function repartirIngreso(montoCentavos: number, cajas: CajaReparto[]): Reparto[] {
  if (!Number.isInteger(montoCentavos) || montoCentavos <= 0) {
    throw new Error('El monto (en centavos) debe ser un entero positivo');
  }
  const total = cajas.reduce((s, c) => s + c.porcentaje, 0);
  if (Math.round(total) !== 100) {
    throw new Error('Los porcentajes de las cajas deben sumar 100');
  }

  const partes: Reparto[] = cajas.map((c) => ({
    cajaId: c.id,
    monto: Math.floor((montoCentavos * c.porcentaje) / 100),
  }));

  const asignado = partes.reduce((s, p) => s + p.monto, 0);
  const residuo = montoCentavos - asignado; // entero >= 0
  if (residuo !== 0) {
    // la caja de mayor porcentaje recibe el residuo entero
    let idxMayor = 0;
    for (let i = 1; i < cajas.length; i++) {
      if (cajas[i].porcentaje > cajas[idxMayor].porcentaje) idxMayor = i;
    }
    partes[idxMayor].monto += residuo;
  }
  return partes;
}
```

- [ ] **Step 4: Ejecutar y verificar que pasa**

Run: `npm test -- reparto`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add src/utils/reparto.ts src/utils/__tests__/reparto.test.ts
git commit -m "feat: reparto de ingresos por porcentaje con manejo de residuo (TDD)"
```

---

## Task 4: Validación de cajas con Zod (TDD)

**Files:**
- Create: `src/features/cajas/cajasSchema.ts`, `src/features/cajas/__tests__/cajasSchema.test.ts`

**Interfaces:**
- Produces: `nuevaCajaSchema`, `validarSumaPorcentajes(porcentajes: number[]): void`

- [ ] **Step 1: Escribir los tests**

`src/features/cajas/__tests__/cajasSchema.test.ts`:
```ts
import { nuevaCajaSchema, validarSumaPorcentajes } from '../cajasSchema';

describe('nuevaCajaSchema', () => {
  it('acepta una caja válida', () => {
    expect(() => nuevaCajaSchema.parse({ nombre: 'Viajes', porcentaje: 10 })).not.toThrow();
  });
  it('rechaza nombre vacío', () => {
    expect(() => nuevaCajaSchema.parse({ nombre: '', porcentaje: 10 })).toThrow();
  });
  it('rechaza porcentaje fuera de 0..100', () => {
    expect(() => nuevaCajaSchema.parse({ nombre: 'X', porcentaje: 120 })).toThrow();
  });
});

describe('validarSumaPorcentajes', () => {
  it('pasa cuando suman 100', () => {
    expect(() => validarSumaPorcentajes([50, 20, 30])).not.toThrow();
  });
  it('lanza cuando no suman 100', () => {
    expect(() => validarSumaPorcentajes([50, 20, 20])).toThrow();
  });
});
```

- [ ] **Step 2: Ejecutar y verificar que falla**

Run: `npm test -- cajasSchema`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementar `cajasSchema.ts`**

`src/features/cajas/cajasSchema.ts`:
```ts
import { z } from 'zod';

export const nuevaCajaSchema = z.object({
  nombre: z.string().trim().min(1, 'El nombre es obligatorio').max(40),
  porcentaje: z.number().min(0).max(100),
});

export function validarSumaPorcentajes(porcentajes: number[]): void {
  const suma = Math.round(porcentajes.reduce((s, p) => s + p, 0));
  if (suma !== 100) {
    throw new Error(`Los porcentajes deben sumar 100 (suman ${suma})`);
  }
}
```

- [ ] **Step 4: Ejecutar y verificar que pasa**

Run: `npm test -- cajasSchema`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/features/cajas/cajasSchema.ts src/features/cajas/__tests__/cajasSchema.test.ts
git commit -m "feat: validación de cajas y suma de porcentajes con Zod (TDD)"
```

---

## Task 5: Inicialización de Firebase + config del Emulator

**Files:**
- Create: `src/lib/firebase.ts`, `firebase.json`, `.env.example`
- Modify: `.gitignore` (añadir `.env`)

**Interfaces:**
- Produces: `app`, `auth`, `db` exportados; `conectarEmuladores()`

- [ ] **Step 1: Crear `firebase.ts`**

`src/lib/firebase.ts`:
```ts
import { initializeApp, getApps } from 'firebase/app';
import {
  initializeAuth,
  getReactNativePersistence,
  connectAuthEmulator,
} from 'firebase/auth';
import { initializeFirestore, connectFirestoreEmulator } from 'firebase/firestore';
import AsyncStorage from '@react-native-async-storage/async-storage';

const firebaseConfig = {
  apiKey: process.env.EXPO_PUBLIC_FB_API_KEY!,
  authDomain: process.env.EXPO_PUBLIC_FB_AUTH_DOMAIN!,
  projectId: process.env.EXPO_PUBLIC_FB_PROJECT_ID!,
  storageBucket: process.env.EXPO_PUBLIC_FB_STORAGE_BUCKET!,
  messagingSenderId: process.env.EXPO_PUBLIC_FB_SENDER_ID!,
  appId: process.env.EXPO_PUBLIC_FB_APP_ID!,
};

export const app = getApps().length ? getApps()[0] : initializeApp(firebaseConfig);
export const auth = initializeAuth(app, {
  persistence: getReactNativePersistence(AsyncStorage),
});
export const db = initializeFirestore(app, {
  experimentalForceLongPolling: true, // estabilidad en RN
});

export function conectarEmuladores() {
  connectAuthEmulator(auth, 'http://127.0.0.1:9099', { disableWarnings: true });
  connectFirestoreEmulator(db, '127.0.0.1', 8080);
}
```

- [ ] **Step 2: Crear `firebase.json` y `.env.example`**

`firebase.json`:
```json
{
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "ui": { "enabled": true }
  },
  "firestore": { "rules": "firestore.rules" }
}
```
`.env.example`:
```
EXPO_PUBLIC_FB_API_KEY=
EXPO_PUBLIC_FB_AUTH_DOMAIN=
EXPO_PUBLIC_FB_PROJECT_ID=
EXPO_PUBLIC_FB_STORAGE_BUCKET=
EXPO_PUBLIC_FB_SENDER_ID=
EXPO_PUBLIC_FB_APP_ID=
```
Crear el proyecto real en console.firebase.google.com, copiar valores a `.env` (no commiteado).

- [ ] **Step 3: Verificar que compila (typecheck)**

Run: `npx tsc --noEmit`
Expected: sin errores en `firebase.ts`.

- [ ] **Step 4: Commit**

```bash
git add src/lib/firebase.ts firebase.json .env.example .gitignore
git commit -m "feat: init de Firebase (Auth + Firestore) y config de emuladores"
```

---

## Task 6: cajasService — crear cajas por defecto y CRUD (test con Emulator)

**Files:**
- Create: `src/features/cajas/cajasService.ts`, `src/features/cajas/__tests__/cajasService.emulator.test.ts`

**Interfaces:**
- Consumes: `db` (Task 5), `Caja`, `NuevaCaja` (Task 2), `validarSumaPorcentajes` (Task 4)
- Produces:
  - `crearCajasPorDefecto(uid: string): Promise<void>`
  - `crearCaja(uid: string, caja: NuevaCaja, porcentajesActuales: number[]): Promise<string>`
  - `actualizarPorcentajes(uid: string, cambios: {id: string; porcentaje: number}[]): Promise<void>`
  - `listarCajas(uid: string): Promise<Caja[]>`
  - `refCajas(uid: string)` (CollectionReference, para useCajas)

- [ ] **Step 1: Escribir el test de emulador**

`src/features/cajas/__tests__/cajasService.emulator.test.ts`:
```ts
import { conectarEmuladores } from '../../../lib/firebase';
import { crearCajasPorDefecto, listarCajas } from '../cajasService';

beforeAll(() => conectarEmuladores());

it('crea las 3 cajas por defecto con 50/20/30', async () => {
  const uid = 'user-test-1';
  await crearCajasPorDefecto(uid);
  const cajas = await listarCajas(uid);
  expect(cajas).toHaveLength(3);
  const porNombre = Object.fromEntries(cajas.map((c) => [c.nombre, c.porcentaje]));
  expect(porNombre).toEqual({ Gastos: 50, Inversión: 20, Ahorro: 30 });
  expect(cajas.every((c) => c.saldo === 0 && c.esPorDefecto)).toBe(true);
});
```

- [ ] **Step 2: Ejecutar contra el emulador y verificar que falla**

Run (terminal A): `firebase emulators:start` (requiere `npm i -g firebase-tools` y `firebase login`).
Run (terminal B): `npm test -- cajasService`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementar `cajasService.ts`**

`src/features/cajas/cajasService.ts`:
```ts
import {
  collection, doc, getDocs, writeBatch, addDoc, query, orderBy,
} from 'firebase/firestore';
import { db } from '../../lib/firebase';
import { Caja, NuevaCaja } from '../../types/models';
import { validarSumaPorcentajes } from './cajasSchema';

export const refCajas = (uid: string) => collection(db, 'users', uid, 'cajas');

const DEFECTO: Omit<Caja, 'id' | 'createdAt'>[] = [
  { nombre: 'Gastos', porcentaje: 50, saldo: 0, esPorDefecto: true, orden: 0 },
  { nombre: 'Inversión', porcentaje: 20, saldo: 0, esPorDefecto: true, orden: 1 },
  { nombre: 'Ahorro', porcentaje: 30, saldo: 0, esPorDefecto: true, orden: 2 },
];

export async function crearCajasPorDefecto(uid: string): Promise<void> {
  const batch = writeBatch(db);
  const now = Date.now();
  for (const c of DEFECTO) {
    const ref = doc(refCajas(uid));
    batch.set(ref, { ...c, createdAt: now });
  }
  await batch.commit();
}

export async function listarCajas(uid: string): Promise<Caja[]> {
  const snap = await getDocs(query(refCajas(uid), orderBy('orden')));
  return snap.docs.map((d) => ({ id: d.id, ...(d.data() as Omit<Caja, 'id'>) }));
}

export async function crearCaja(
  uid: string, caja: NuevaCaja, porcentajesActuales: number[],
): Promise<string> {
  validarSumaPorcentajes([...porcentajesActuales, caja.porcentaje]);
  const ref = await addDoc(refCajas(uid), {
    nombre: caja.nombre, porcentaje: caja.porcentaje, saldo: 0,
    esPorDefecto: false, orden: porcentajesActuales.length, createdAt: Date.now(),
  });
  return ref.id;
}

export async function actualizarPorcentajes(
  uid: string, cambios: { id: string; porcentaje: number }[],
): Promise<void> {
  validarSumaPorcentajes(cambios.map((c) => c.porcentaje));
  const batch = writeBatch(db);
  for (const c of cambios) {
    batch.update(doc(refCajas(uid), c.id), { porcentaje: c.porcentaje });
  }
  await batch.commit();
}
```

- [ ] **Step 4: Ejecutar y verificar que pasa**

Run: `npm test -- cajasService`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/features/cajas/cajasService.ts src/features/cajas/__tests__/cajasService.emulator.test.ts
git commit -m "feat: cajasService con cajas por defecto y CRUD (test emulador)"
```

---

## Task 7: transaccionesService — ingreso/egreso/borrar con saldos atómicos (test con Emulator)

**Files:**
- Create: `src/features/transacciones/transaccionesService.ts`, `src/features/transacciones/__tests__/transaccionesService.emulator.test.ts`

**Interfaces:**
- Consumes: `db`, `repartirIngreso` (Task 3), `Caja`, `Transaccion` (Task 2), `crearCajasPorDefecto`, `listarCajas` (Task 6)
- Produces:
  - `agregarIngreso(uid, {monto, descripcion, fecha}): Promise<void>`
  - `agregarEgreso(uid, {monto, cajaId, descripcion, fecha}): Promise<{advertenciaSaldo: boolean}>`
  - `borrarTransaccion(uid, txId): Promise<void>`
  - `refTransacciones(uid)` (para useTransacciones)

- [ ] **Step 1: Escribir los tests de emulador**

`src/features/transacciones/__tests__/transaccionesService.emulator.test.ts`:
```ts
import { conectarEmuladores } from '../../../lib/firebase';
import { crearCajasPorDefecto, listarCajas } from '../../cajas/cajasService';
import { agregarIngreso, agregarEgreso } from '../transaccionesService';

beforeAll(() => conectarEmuladores());

it('un ingreso reparte por % y sube los saldos', async () => {
  const uid = 'tx-user-1';
  await crearCajasPorDefecto(uid);
  await agregarIngreso(uid, { monto: 100, descripcion: 'Sueldo', fecha: 1 });
  const cajas = await listarCajas(uid);
  const s = Object.fromEntries(cajas.map((c) => [c.nombre, c.saldo]));
  expect(s).toEqual({ Gastos: 50, Inversión: 20, Ahorro: 30 });
});

it('un egreso baja el saldo de su caja y avisa si excede', async () => {
  const uid = 'tx-user-2';
  await crearCajasPorDefecto(uid);
  await agregarIngreso(uid, { monto: 100, descripcion: 'x', fecha: 1 });
  const gastos = (await listarCajas(uid)).find((c) => c.nombre === 'Gastos')!;
  const r = await agregarEgreso(uid, {
    monto: 80, cajaId: gastos.id, descripcion: 'Mercado', fecha: 2,
  });
  expect(r.advertenciaSaldo).toBe(true); // 80 > 50
  const gastos2 = (await listarCajas(uid)).find((c) => c.nombre === 'Gastos')!;
  expect(gastos2.saldo).toBe(-30);
});
```

- [ ] **Step 2: Ejecutar contra el emulador y verificar que falla**

Run: `npm test -- transaccionesService`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementar `transaccionesService.ts`**

`src/features/transacciones/transaccionesService.ts`:
```ts
import {
  collection, doc, writeBatch, getDoc, getDocs, increment,
} from 'firebase/firestore';
import { db } from '../../lib/firebase';
import { repartirIngreso } from '../../utils/reparto';
import { refCajas } from '../cajas/cajasService';
import { Caja, Transaccion } from '../../types/models';

export const refTransacciones = (uid: string) =>
  collection(db, 'users', uid, 'transacciones');

export async function agregarIngreso(
  uid: string,
  { monto, descripcion, fecha }: { monto: number; descripcion: string; fecha: number },
): Promise<void> {
  const snap = await getDocs(refCajas(uid));
  const cajas = snap.docs.map((d) => ({ id: d.id, ...(d.data() as Omit<Caja, 'id'>) }));
  const reparto = repartirIngreso(monto, cajas);

  const batch = writeBatch(db);
  const txRef = doc(refTransacciones(uid));
  batch.set(txRef, {
    tipo: 'ingreso', monto, fecha, descripcion, cajaId: null,
    reparto, createdAt: Date.now(),
  } as Omit<Transaccion, 'id'>);
  for (const p of reparto) {
    batch.update(doc(refCajas(uid), p.cajaId), { saldo: increment(p.monto) });
  }
  await batch.commit();
}

export async function agregarEgreso(
  uid: string,
  { monto, cajaId, descripcion, fecha }:
    { monto: number; cajaId: string; descripcion: string; fecha: number },
): Promise<{ advertenciaSaldo: boolean }> {
  const cajaSnap = await getDoc(doc(refCajas(uid), cajaId));
  const saldoActual = (cajaSnap.data() as Caja | undefined)?.saldo ?? 0;
  const advertenciaSaldo = monto > saldoActual;

  const batch = writeBatch(db);
  const txRef = doc(refTransacciones(uid));
  batch.set(txRef, {
    tipo: 'egreso', monto, fecha, descripcion, cajaId,
    reparto: [], createdAt: Date.now(),
  } as Omit<Transaccion, 'id'>);
  batch.update(doc(refCajas(uid), cajaId), { saldo: increment(-monto) });
  await batch.commit();
  return { advertenciaSaldo };
}

export async function borrarTransaccion(uid: string, txId: string): Promise<void> {
  const txSnap = await getDoc(doc(refTransacciones(uid), txId));
  const tx = txSnap.data() as Transaccion | undefined;
  if (!tx) return;
  const batch = writeBatch(db);
  if (tx.tipo === 'ingreso') {
    for (const p of tx.reparto) {
      batch.update(doc(refCajas(uid), p.cajaId), { saldo: increment(-p.monto) });
    }
  } else if (tx.cajaId) {
    batch.update(doc(refCajas(uid), tx.cajaId), { saldo: increment(tx.monto) });
  }
  batch.delete(doc(refTransacciones(uid), txId));
  await batch.commit();
}
```

- [ ] **Step 4: Ejecutar y verificar que pasa**

Run: `npm test -- transaccionesService`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add src/features/transacciones/transaccionesService.ts src/features/transacciones/__tests__/transaccionesService.emulator.test.ts
git commit -m "feat: transaccionesService (ingreso/egreso/borrar) con saldos atómicos (test emulador)"
```

---

## Task 8: Security Rules + test de reglas

**Files:**
- Ya existe (creado en Task 5): `firestore.rules` — verificar/ajustar si hace falta
- Create: `src/__tests__/rules.emulator.test.ts`

**Interfaces:**
- Produces: tests que confirman que las reglas limitan a `users/{uid}/**` y validan datos.

**Nota:** `firestore.rules` ya fue creado en la Task 5 (con las reglas reales restrictivas por usuario), porque el emulador lo necesita para arrancar en las Tasks 6/7. Esta tarea NO lo recrea: revisa que el contenido sea el esperado y escribe los tests de reglas.

- [ ] **Step 1: Verificar `firestore.rules` (ya existe; debe contener esto)**

`firestore.rules`:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /cajas/{cajaId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      match /transacciones/{txId} {
        allow read: if request.auth != null && request.auth.uid == userId;
        allow create: if request.auth != null && request.auth.uid == userId
          && request.resource.data.monto > 0
          && request.resource.data.tipo in ['ingreso', 'egreso'];
        allow update, delete: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
```

- [ ] **Step 2: Escribir el test de reglas**

`src/__tests__/rules.emulator.test.ts`:
```ts
import { initializeTestEnvironment, assertFails, assertSucceeds }
  from '@firebase/rules-unit-testing';
import { doc, getDoc, setDoc } from 'firebase/firestore';
import { readFileSync } from 'fs';

let env: Awaited<ReturnType<typeof initializeTestEnvironment>>;

beforeAll(async () => {
  env = await initializeTestEnvironment({
    projectId: 'demo-hugobox',
    firestore: { rules: readFileSync('firestore.rules', 'utf8'), host: '127.0.0.1', port: 8080 },
  });
});
afterAll(() => env.cleanup());

it('un usuario NO puede leer cajas de otro', async () => {
  const ana = env.authenticatedContext('ana').firestore();
  await assertFails(getDoc(doc(ana, 'users/beto/cajas/x')));
});

it('un usuario SÍ puede escribir sus propias cajas', async () => {
  const ana = env.authenticatedContext('ana').firestore();
  await assertSucceeds(setDoc(doc(ana, 'users/ana/cajas/x'), { nombre: 'Gastos', porcentaje: 50, saldo: 0, esPorDefecto: true, orden: 0, createdAt: 1 }));
});
```
Instalar dev dep: `npm i -D @firebase/rules-unit-testing`.

- [ ] **Step 3: Ejecutar y verificar (rojo→verde)**

Run (con emulador activo): `npm test -- rules`
Expected: PASS. Si falla por reglas, ajustar `firestore.rules` hasta pasar.

- [ ] **Step 4: Commit**

```bash
git add firestore.rules src/__tests__/rules.emulator.test.ts package.json
git commit -m "feat: Security Rules por usuario + tests de reglas"
```

---

## Task 9: Auth — Google Sign-In, store de sesión y bootstrap de usuario

**Files:**
- Create: `src/stores/sessionStore.ts`, `src/features/auth/authService.ts`, `src/features/auth/useAuth.ts`
- Modify: `app.json` (plugin de google-signin), `jest.setup.ts` (mock de google-signin)

**Interfaces:**
- Consumes: `auth`, `db` (Task 5), `crearCajasPorDefecto` (Task 6), `Usuario` (Task 2)
- Produces:
  - `sessionStore` (Zustand): `{ usuario: Usuario | null; cargando: boolean; setUsuario; }`
  - `entrarConGoogle(): Promise<void>`, `cerrarSesion(): Promise<void>`
  - `useAuth(): { usuario, cargando }` (suscribe a `onAuthStateChanged` y crea doc+cajas si es nuevo)

- [ ] **Step 1: Crear el store de sesión**

`src/stores/sessionStore.ts`:
```ts
import { create } from 'zustand';
import { Usuario } from '../types/models';

interface SessionState {
  usuario: Usuario | null;
  cargando: boolean;
  setUsuario: (u: Usuario | null) => void;
  setCargando: (v: boolean) => void;
}

export const useSessionStore = create<SessionState>((set) => ({
  usuario: null,
  cargando: true,
  setUsuario: (usuario) => set({ usuario }),
  setCargando: (cargando) => set({ cargando }),
}));
```

- [ ] **Step 2: Crear `authService.ts`**

`src/features/auth/authService.ts`:
```ts
import { GoogleSignin } from '@react-native-google-signin/google-signin';
import { GoogleAuthProvider, signInWithCredential, signOut } from 'firebase/auth';
import { doc, getDoc, setDoc } from 'firebase/firestore';
import { auth, db } from '../../lib/firebase';
import { crearCajasPorDefecto } from '../cajas/cajasService';
import { Usuario } from '../../types/models';

export function configurarGoogle() {
  GoogleSignin.configure({
    webClientId: process.env.EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID!,
  });
}

export async function entrarConGoogle(): Promise<void> {
  await GoogleSignin.hasPlayServices();
  const { idToken } = await GoogleSignin.signIn();
  const cred = GoogleAuthProvider.credential(idToken);
  await signInWithCredential(auth, cred);
}

export async function cerrarSesion(): Promise<void> {
  await GoogleSignin.signOut().catch(() => {});
  await signOut(auth);
}

/** Crea el doc del usuario y sus cajas si es su primer ingreso. */
export async function asegurarUsuario(u: {
  uid: string; email: string | null; displayName: string | null;
}): Promise<Usuario> {
  const ref = doc(db, 'users', u.uid);
  const snap = await getDoc(ref);
  if (!snap.exists()) {
    const nuevo: Usuario = {
      uid: u.uid, email: u.email, displayName: u.displayName,
      monedaPreferida: 'COP', createdAt: Date.now(),
    };
    await setDoc(ref, nuevo);
    await crearCajasPorDefecto(u.uid);
    return nuevo;
  }
  return snap.data() as Usuario;
}
```
Añadir a `.env`: `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=` (del OAuth client web en Google Cloud).

- [ ] **Step 3: Crear `useAuth.ts`**

`src/features/auth/useAuth.ts`:
```ts
import { useEffect } from 'react';
import { onAuthStateChanged } from 'firebase/auth';
import { auth } from '../../lib/firebase';
import { useSessionStore } from '../../stores/sessionStore';
import { asegurarUsuario } from './authService';

export function useAuth() {
  const { usuario, cargando, setUsuario, setCargando } = useSessionStore();
  useEffect(() => {
    const unsub = onAuthStateChanged(auth, async (fbUser) => {
      if (fbUser) {
        const u = await asegurarUsuario({
          uid: fbUser.uid, email: fbUser.email, displayName: fbUser.displayName,
        });
        setUsuario(u);
      } else {
        setUsuario(null);
      }
      setCargando(false);
    });
    return unsub;
  }, []);
  return { usuario, cargando };
}
```

- [ ] **Step 4: Configurar plugin y mock**

En `app.json`, dentro de `"plugins"`, añadir `"@react-native-google-signin/google-signin"`.
En `jest.setup.ts`, añadir:
```ts
jest.mock('@react-native-google-signin/google-signin', () => ({
  GoogleSignin: {
    configure: jest.fn(), hasPlayServices: jest.fn(), signIn: jest.fn(), signOut: jest.fn(),
  },
}));
```

- [ ] **Step 5: Typecheck**

Run: `npx tsc --noEmit`
Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: auth Google + store de sesión + bootstrap de usuario y cajas"
```

---

## Task 10: Navegación Expo Router + guard de sesión

**Files:**
- Create/Modify: `app/_layout.tsx`, `app/(auth)/login.tsx`, `app/(tabs)/_layout.tsx`
- Modify: `package.json` (`"main": "expo-router/entry"`), `app.json` (scheme + plugin expo-router)

**Interfaces:**
- Consumes: `useAuth` (Task 9), `entrarConGoogle`, `configurarGoogle` (Task 9)

- [ ] **Step 1: Configurar expo-router**

En `package.json`: `"main": "expo-router/entry"`.
En `app.json`: añadir `"scheme": "hugobox"` y el plugin `"expo-router"`.

- [ ] **Step 2: Root layout con guard**

`app/_layout.tsx`:
```tsx
import { useEffect } from 'react';
import { Slot, useRouter, useSegments } from 'expo-router';
import { ActivityIndicator, View } from 'react-native';
import { useAuth } from '../src/features/auth/useAuth';
import { configurarGoogle } from '../src/features/auth/authService';

export default function RootLayout() {
  const { usuario, cargando } = useAuth();
  const segments = useSegments();
  const router = useRouter();

  useEffect(() => { configurarGoogle(); }, []);
  useEffect(() => {
    if (cargando) return;
    const enAuth = segments[0] === '(auth)';
    if (!usuario && !enAuth) router.replace('/(auth)/login');
    else if (usuario && enAuth) router.replace('/(tabs)');
  }, [usuario, cargando, segments]);

  if (cargando) {
    return <View style={{ flex: 1, justifyContent: 'center' }}><ActivityIndicator /></View>;
  }
  return <Slot />;
}
```

- [ ] **Step 3: Pantalla de login**

`app/(auth)/login.tsx`:
```tsx
import { useState } from 'react';
import { View, Text, Pressable, StyleSheet, Alert } from 'react-native';
import { entrarConGoogle } from '../../src/features/auth/authService';

export default function Login() {
  const [cargando, setCargando] = useState(false);
  const onPress = async () => {
    try { setCargando(true); await entrarConGoogle(); }
    catch (e) { Alert.alert('No se pudo iniciar sesión', String(e)); }
    finally { setCargando(false); }
  };
  return (
    <View style={s.c}>
      <Text style={s.titulo}>HugoBox</Text>
      <Text style={s.sub}>Tu presupuesto por cajas</Text>
      <Pressable style={s.btn} onPress={onPress} disabled={cargando}>
        <Text style={s.btnTxt}>{cargando ? 'Entrando…' : 'Continuar con Google'}</Text>
      </Pressable>
    </View>
  );
}
const s = StyleSheet.create({
  c: { flex: 1, alignItems: 'center', justifyContent: 'center', gap: 8, padding: 24 },
  titulo: { fontSize: 32, fontWeight: '700' },
  sub: { fontSize: 16, color: '#666', marginBottom: 24 },
  btn: { backgroundColor: '#1a73e8', paddingVertical: 14, paddingHorizontal: 28, borderRadius: 10 },
  btnTxt: { color: 'white', fontWeight: '600', fontSize: 16 },
});
```

- [ ] **Step 4: Tabs layout**

`app/(tabs)/_layout.tsx`:
```tsx
import { Tabs } from 'expo-router';
export default function TabsLayout() {
  return (
    <Tabs>
      <Tabs.Screen name="index" options={{ title: 'Cajas' }} />
      <Tabs.Screen name="historial" options={{ title: 'Historial' }} />
    </Tabs>
  );
}
```

- [ ] **Step 5: Probar en development build**

Run: `npx expo run:android` (development build; Google Sign-In no funciona en Expo Go).
Expected: arranca en login; tras Google entra a tabs. Si el OAuth no está listo, validar al menos que se ve la pantalla de login.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: navegación expo-router con guard de sesión y pantalla de login"
```

---

## Task 11: Dashboard de cajas (useCajas + CajaCard)

**Files:**
- Create: `src/features/cajas/useCajas.ts`, `src/components/CajaCard.tsx`, `app/(tabs)/index.tsx`

**Interfaces:**
- Consumes: `refCajas` (Task 6), `useSessionStore` (Task 9), `Caja` (Task 2), `formatearMoneda` (Task 1)
- Produces: `useCajas(): { cajas: Caja[]; cargando: boolean }`

- [ ] **Step 1: Hook con onSnapshot**

`src/features/cajas/useCajas.ts`:
```ts
import { useEffect, useState } from 'react';
import { onSnapshot, query, orderBy } from 'firebase/firestore';
import { refCajas } from './cajasService';
import { useSessionStore } from '../../stores/sessionStore';
import { Caja } from '../../types/models';

export function useCajas() {
  const uid = useSessionStore((s) => s.usuario?.uid);
  const [cajas, setCajas] = useState<Caja[]>([]);
  const [cargando, setCargando] = useState(true);
  useEffect(() => {
    if (!uid) return;
    const unsub = onSnapshot(query(refCajas(uid), orderBy('orden')), (snap) => {
      setCajas(snap.docs.map((d) => ({ id: d.id, ...(d.data() as Omit<Caja, 'id'>) })));
      setCargando(false);
    });
    return unsub;
  }, [uid]);
  return { cajas, cargando };
}
```

- [ ] **Step 2: Componente CajaCard**

`src/components/CajaCard.tsx`:
```tsx
import { View, Text, StyleSheet } from 'react-native';
import { Caja } from '../types/models';
import { formatearMoneda } from '../utils/dinero';

export function CajaCard({ caja }: { caja: Caja }) {
  const negativo = caja.saldo < 0;
  return (
    <View style={s.card}>
      <View style={s.row}>
        <Text style={s.nombre}>{caja.nombre}</Text>
        <Text style={s.pct}>{caja.porcentaje}%</Text>
      </View>
      <Text style={[s.saldo, negativo && s.neg]}>{formatearMoneda(caja.saldo)}</Text>
    </View>
  );
}
const s = StyleSheet.create({
  card: { backgroundColor: '#fff', borderRadius: 12, padding: 16, marginBottom: 12, elevation: 2 },
  row: { flexDirection: 'row', justifyContent: 'space-between' },
  nombre: { fontSize: 16, fontWeight: '600' },
  pct: { color: '#888' },
  saldo: { fontSize: 22, fontWeight: '700', marginTop: 8 },
  neg: { color: '#d32f2f' },
});
```

- [ ] **Step 3: Pantalla dashboard**

`app/(tabs)/index.tsx`:
```tsx
import { View, FlatList, Pressable, Text, StyleSheet } from 'react-native';
import { useRouter } from 'expo-router';
import { useCajas } from '../../src/features/cajas/useCajas';
import { CajaCard } from '../../src/components/CajaCard';

export default function Cajas() {
  const { cajas } = useCajas();
  const router = useRouter();
  return (
    <View style={s.c}>
      <FlatList
        data={cajas}
        keyExtractor={(c) => c.id}
        renderItem={({ item }) => <CajaCard caja={item} />}
        contentContainerStyle={{ padding: 16 }}
      />
      <Pressable style={s.fab} onPress={() => router.push('/transaccion/nueva')}>
        <Text style={s.fabTxt}>＋ Movimiento</Text>
      </Pressable>
    </View>
  );
}
const s = StyleSheet.create({
  c: { flex: 1, backgroundColor: '#f4f5f7' },
  fab: { position: 'absolute', bottom: 24, right: 24, backgroundColor: '#1a73e8', borderRadius: 28, paddingVertical: 14, paddingHorizontal: 20 },
  fabTxt: { color: 'white', fontWeight: '700' },
});
```

- [ ] **Step 4: Probar**

Run: app en development build. Expected: se ven las 3 cajas con saldo 0 al crear cuenta.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: dashboard de cajas en tiempo real (useCajas + CajaCard)"
```

---

## Task 12: Pantalla "nueva transacción" (ingreso/egreso) con RHF + Zod

**Files:**
- Create: `src/features/transacciones/txSchema.ts`, `src/features/transacciones/useTransacciones.ts`, `app/transaccion/nueva.tsx`

**Interfaces:**
- Consumes: `agregarIngreso`, `agregarEgreso` (Task 7), `useCajas` (Task 11), `useSessionStore` (Task 9)
- Produces: `useTransacciones()` con `crearIngreso`, `crearEgreso`; `txFormSchema`

- [ ] **Step 1: Schema de formulario (TDD)**

`src/features/transacciones/txSchema.ts`:
```ts
import { z } from 'zod';
export const txFormSchema = z.object({
  tipo: z.enum(['ingreso', 'egreso']),
  monto: z.number().positive('El monto debe ser mayor a 0'),
  descripcion: z.string().trim().max(80).default(''),
  cajaId: z.string().nullable(),
}).refine((d) => d.tipo === 'ingreso' || !!d.cajaId, {
  message: 'Elige una caja para el egreso', path: ['cajaId'],
});
export type TxForm = z.infer<typeof txFormSchema>;
```
Test `src/features/transacciones/__tests__/txSchema.test.ts`:
```ts
import { txFormSchema } from '../txSchema';
it('exige caja en egreso', () => {
  expect(() => txFormSchema.parse({ tipo: 'egreso', monto: 10, descripcion: '', cajaId: null })).toThrow();
});
it('ingreso no exige caja', () => {
  expect(() => txFormSchema.parse({ tipo: 'ingreso', monto: 10, descripcion: '', cajaId: null })).not.toThrow();
});
```
Run: `npm test -- txSchema` → PASS.

- [ ] **Step 2: Hook useTransacciones**

`src/features/transacciones/useTransacciones.ts`:
```ts
import { Alert } from 'react-native';
import { useSessionStore } from '../../stores/sessionStore';
import { agregarIngreso, agregarEgreso } from './transaccionesService';

export function useTransacciones() {
  const uid = useSessionStore((s) => s.usuario?.uid);
  async function crearIngreso(monto: number, descripcion: string) {
    if (!uid) return;
    await agregarIngreso(uid, { monto, descripcion, fecha: Date.now() });
  }
  async function crearEgreso(monto: number, cajaId: string, descripcion: string) {
    if (!uid) return;
    const { advertenciaSaldo } = await agregarEgreso(uid, { monto, cajaId, descripcion, fecha: Date.now() });
    if (advertenciaSaldo) {
      Alert.alert('Saldo insuficiente', 'Registramos el egreso, pero esta caja quedó en negativo.');
    }
  }
  return { crearIngreso, crearEgreso };
}
```

- [ ] **Step 3: Pantalla del formulario**

`app/transaccion/nueva.tsx`:
```tsx
import { useState } from 'react';
import { View, Text, TextInput, Pressable, StyleSheet, ScrollView } from 'react-native';
import { useRouter } from 'expo-router';
import { useCajas } from '../../src/features/cajas/useCajas';
import { useTransacciones } from '../../src/features/transacciones/useTransacciones';
import { txFormSchema } from '../../src/features/transacciones/txSchema';
import { aCentavos } from '../../src/utils/dinero';

export default function NuevaTx() {
  const { cajas } = useCajas();
  const { crearIngreso, crearEgreso } = useTransacciones();
  const router = useRouter();
  const [tipo, setTipo] = useState<'ingreso' | 'egreso'>('ingreso');
  const [monto, setMonto] = useState('');
  const [descripcion, setDescripcion] = useState('');
  const [cajaId, setCajaId] = useState<string | null>(null);
  const [error, setError] = useState('');

  const guardar = async () => {
    const parsed = txFormSchema.safeParse({ tipo, monto: Number(monto), descripcion, cajaId });
    if (!parsed.success) { setError(parsed.error.issues[0].message); return; }
    // El input está en unidades; los servicios trabajan en centavos enteros.
    const centavos = aCentavos(Number(monto));
    if (tipo === 'ingreso') await crearIngreso(centavos, descripcion);
    else await crearEgreso(centavos, cajaId!, descripcion);
    router.back();
  };

  return (
    <ScrollView contentContainerStyle={s.c}>
      <View style={s.tabs}>
        {(['ingreso', 'egreso'] as const).map((t) => (
          <Pressable key={t} onPress={() => setTipo(t)} style={[s.tab, tipo === t && s.tabOn]}>
            <Text style={tipo === t ? s.tabTxtOn : s.tabTxt}>{t}</Text>
          </Pressable>
        ))}
      </View>
      <TextInput style={s.input} placeholder="Monto" keyboardType="numeric" value={monto} onChangeText={setMonto} />
      <TextInput style={s.input} placeholder="Descripción" value={descripcion} onChangeText={setDescripcion} />
      {tipo === 'egreso' && (
        <View style={s.cajas}>
          {cajas.map((c) => (
            <Pressable key={c.id} onPress={() => setCajaId(c.id)} style={[s.chip, cajaId === c.id && s.chipOn]}>
              <Text style={cajaId === c.id ? s.chipTxtOn : undefined}>{c.nombre}</Text>
            </Pressable>
          ))}
        </View>
      )}
      {!!error && <Text style={s.err}>{error}</Text>}
      <Pressable style={s.btn} onPress={guardar}><Text style={s.btnTxt}>Guardar</Text></Pressable>
    </ScrollView>
  );
}
const s = StyleSheet.create({
  c: { padding: 16, gap: 12 },
  tabs: { flexDirection: 'row', gap: 8 },
  tab: { flex: 1, padding: 12, borderRadius: 8, backgroundColor: '#eee', alignItems: 'center' },
  tabOn: { backgroundColor: '#1a73e8' },
  tabTxt: { color: '#333', textTransform: 'capitalize' },
  tabTxtOn: { color: 'white', textTransform: 'capitalize' },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12 },
  cajas: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 },
  chip: { paddingVertical: 8, paddingHorizontal: 14, borderRadius: 16, backgroundColor: '#eee' },
  chipOn: { backgroundColor: '#1a73e8' },
  chipTxtOn: { color: 'white' },
  err: { color: '#d32f2f' },
  btn: { backgroundColor: '#1a73e8', padding: 14, borderRadius: 10, alignItems: 'center' },
  btnTxt: { color: 'white', fontWeight: '700' },
});
```

- [ ] **Step 4: Probar el flujo**

Run: app. Registrar un ingreso de 100 → cajas suben 50/20/30. Registrar egreso > saldo → muestra la alerta.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: pantalla de nueva transacción (ingreso/egreso) con validación"
```

---

## Task 13: Historial con filtros + borrar

**Files:**
- Create: `src/features/transacciones/useHistorial.ts`, `app/(tabs)/historial.tsx`

**Interfaces:**
- Consumes: `refTransacciones`, `borrarTransaccion` (Task 7), `useCajas` (Task 11), `formatearMoneda` (Task 1)
- Produces: `useHistorial({ cajaId? }): { items: Transaccion[] }`

- [ ] **Step 1: Hook de historial**

`src/features/transacciones/useHistorial.ts`:
```ts
import { useEffect, useState } from 'react';
import { onSnapshot, query, orderBy, where } from 'firebase/firestore';
import { refTransacciones } from './transaccionesService';
import { useSessionStore } from '../../stores/sessionStore';
import { Transaccion } from '../../types/models';

export function useHistorial(filtroCajaId?: string | null) {
  const uid = useSessionStore((s) => s.usuario?.uid);
  const [items, setItems] = useState<Transaccion[]>([]);
  useEffect(() => {
    if (!uid) return;
    const base = refTransacciones(uid);
    const q = filtroCajaId
      ? query(base, where('cajaId', '==', filtroCajaId), orderBy('fecha', 'desc'))
      : query(base, orderBy('fecha', 'desc'));
    return onSnapshot(q, (snap) =>
      setItems(snap.docs.map((d) => ({ id: d.id, ...(d.data() as Omit<Transaccion, 'id'>) }))));
  }, [uid, filtroCajaId]);
  return { items };
}
```
Nota: el filtro por caja con orden requiere índice compuesto; Firestore mostrará un link para crearlo la primera vez.

- [ ] **Step 2: Pantalla historial**

`app/(tabs)/historial.tsx`:
```tsx
import { useState } from 'react';
import { View, FlatList, Text, Pressable, StyleSheet, Alert } from 'react-native';
import { useHistorial } from '../../src/features/transacciones/useHistorial';
import { useCajas } from '../../src/features/cajas/useCajas';
import { useSessionStore } from '../../src/stores/sessionStore';
import { borrarTransaccion } from '../../src/features/transacciones/transaccionesService';
import { formatearMoneda } from '../../src/utils/dinero';

export default function Historial() {
  const [filtro, setFiltro] = useState<string | null>(null);
  const { items } = useHistorial(filtro);
  const { cajas } = useCajas();
  const uid = useSessionStore((s) => s.usuario?.uid);

  const onBorrar = (id: string) =>
    Alert.alert('Borrar', '¿Eliminar este movimiento? Se revertirán los saldos.', [
      { text: 'Cancelar' },
      { text: 'Borrar', style: 'destructive', onPress: () => uid && borrarTransaccion(uid, id) },
    ]);

  return (
    <View style={s.c}>
      <View style={s.filtros}>
        <Pressable onPress={() => setFiltro(null)} style={[s.chip, !filtro && s.chipOn]}><Text>Todas</Text></Pressable>
        {cajas.map((c) => (
          <Pressable key={c.id} onPress={() => setFiltro(c.id)} style={[s.chip, filtro === c.id && s.chipOn]}>
            <Text>{c.nombre}</Text></Pressable>
        ))}
      </View>
      <FlatList
        data={items}
        keyExtractor={(t) => t.id}
        renderItem={({ item }) => (
          <Pressable onLongPress={() => onBorrar(item.id)} style={s.row}>
            <Text style={s.desc}>{item.descripcion || item.tipo}</Text>
            <Text style={item.tipo === 'ingreso' ? s.in : s.out}>
              {item.tipo === 'ingreso' ? '+' : '-'}{formatearMoneda(item.monto)}
            </Text>
          </Pressable>
        )}
      />
    </View>
  );
}
const s = StyleSheet.create({
  c: { flex: 1, padding: 12 },
  filtros: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginBottom: 8 },
  chip: { paddingVertical: 6, paddingHorizontal: 12, borderRadius: 14, backgroundColor: '#eee' },
  chipOn: { backgroundColor: '#cfe0fc' },
  row: { flexDirection: 'row', justifyContent: 'space-between', paddingVertical: 12, borderBottomWidth: 1, borderColor: '#eee' },
  desc: { fontSize: 15 },
  in: { color: '#2e7d32', fontWeight: '600' },
  out: { color: '#d32f2f', fontWeight: '600' },
});
```

- [ ] **Step 3: Probar**

Run: app. Ver lista, filtrar por caja, mantener pulsado para borrar y verificar que el saldo se revierte.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: historial con filtros por caja y borrado con reversión de saldo"
```

---

## Task 14: Gestión de cajas (crear + editar %)

**Files:**
- Create: `app/cajas/index.tsx`
- Modify: `app/(tabs)/index.tsx` (botón a gestión)

**Interfaces:**
- Consumes: `useCajas` (Task 11), `crearCaja`, `actualizarPorcentajes` (Task 6), `nuevaCajaSchema`, `validarSumaPorcentajes` (Task 4)

- [ ] **Step 1: Pantalla de gestión**

`app/cajas/index.tsx`:
```tsx
import { useEffect, useState } from 'react';
import { View, Text, TextInput, Pressable, StyleSheet, Alert, ScrollView } from 'react-native';
import { useCajas } from '../../src/features/cajas/useCajas';
import { useSessionStore } from '../../src/stores/sessionStore';
import { crearCaja, actualizarPorcentajes } from '../../src/features/cajas/cajasService';
import { validarSumaPorcentajes } from '../../src/features/cajas/cajasSchema';

export default function GestionCajas() {
  const { cajas } = useCajas();
  const uid = useSessionStore((s) => s.usuario?.uid)!;
  const [pcts, setPcts] = useState<Record<string, string>>({});
  const [nombre, setNombre] = useState('');
  const [nuevoPct, setNuevoPct] = useState('');

  useEffect(() => {
    setPcts(Object.fromEntries(cajas.map((c) => [c.id, String(c.porcentaje)])));
  }, [cajas.length]);

  const guardarPcts = async () => {
    const cambios = cajas.map((c) => ({ id: c.id, porcentaje: Number(pcts[c.id] ?? c.porcentaje) }));
    try {
      validarSumaPorcentajes(cambios.map((c) => c.porcentaje));
      await actualizarPorcentajes(uid, cambios);
      Alert.alert('Listo', 'Porcentajes actualizados');
    } catch (e) { Alert.alert('Revisa los %', String((e as Error).message)); }
  };

  const agregar = async () => {
    try {
      await crearCaja(uid, { nombre, porcentaje: Number(nuevoPct) }, cajas.map((c) => c.porcentaje));
      setNombre(''); setNuevoPct('');
    } catch (e) { Alert.alert('No se pudo crear', String((e as Error).message)); }
  };

  return (
    <ScrollView contentContainerStyle={s.c}>
      <Text style={s.h}>Porcentajes (deben sumar 100)</Text>
      {cajas.map((c) => (
        <View key={c.id} style={s.row}>
          <Text style={s.nombre}>{c.nombre}</Text>
          <TextInput style={s.pct} keyboardType="numeric" value={pcts[c.id] ?? ''} onChangeText={(v) => setPcts((p) => ({ ...p, [c.id]: v }))} />
          <Text>%</Text>
        </View>
      ))}
      <Pressable style={s.btn} onPress={guardarPcts}><Text style={s.btnTxt}>Guardar %</Text></Pressable>

      <Text style={s.h}>Nueva caja</Text>
      <TextInput style={s.input} placeholder="Nombre" value={nombre} onChangeText={setNombre} />
      <TextInput style={s.input} placeholder="Porcentaje" keyboardType="numeric" value={nuevoPct} onChangeText={setNuevoPct} />
      <Pressable style={s.btn} onPress={agregar}><Text style={s.btnTxt}>Agregar caja</Text></Pressable>
    </ScrollView>
  );
}
const s = StyleSheet.create({
  c: { padding: 16, gap: 10 },
  h: { fontSize: 16, fontWeight: '700', marginTop: 12 },
  row: { flexDirection: 'row', alignItems: 'center', gap: 8 },
  nombre: { flex: 1 },
  pct: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 8, width: 64, textAlign: 'right' },
  input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 8, padding: 12 },
  btn: { backgroundColor: '#1a73e8', padding: 12, borderRadius: 10, alignItems: 'center', marginTop: 6 },
  btnTxt: { color: 'white', fontWeight: '700' },
});
```

- [ ] **Step 2: Enlace desde el dashboard**

En `app/(tabs)/index.tsx`, añadir en la cabecera un botón:
```tsx
// dentro del componente, encima del FlatList:
<Pressable onPress={() => router.push('/cajas')} style={{ padding: 16 }}>
  <Text style={{ color: '#1a73e8', fontWeight: '600' }}>Gestionar cajas</Text>
</Pressable>
```

- [ ] **Step 3: Probar**

Run: app. Cambiar % a algo que no sume 100 → error. Sumar 100 → guarda. Crear caja nueva con % que rompa 100 → error.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: gestión de cajas (crear y editar porcentajes con validación 100%)"
```

---

## Task 15: Error Boundary global y manejo de carga

**Files:**
- Create: `src/components/ErrorBoundary.tsx`
- Modify: `app/_layout.tsx` (envolver `<Slot/>`)

**Interfaces:**
- Produces: `<ErrorBoundary>` que captura errores de render y muestra fallback.

- [ ] **Step 1: ErrorBoundary**

`src/components/ErrorBoundary.tsx`:
```tsx
import React from 'react';
import { View, Text, Pressable, StyleSheet } from 'react-native';

export class ErrorBoundary extends React.Component<
  { children: React.ReactNode }, { error: Error | null }
> {
  state = { error: null as Error | null };
  static getDerivedStateFromError(error: Error) { return { error }; }
  render() {
    if (this.state.error) {
      return (
        <View style={s.c}>
          <Text style={s.t}>Algo salió mal</Text>
          <Text style={s.m}>{this.state.error.message}</Text>
          <Pressable style={s.b} onPress={() => this.setState({ error: null })}>
            <Text style={s.bt}>Reintentar</Text>
          </Pressable>
        </View>
      );
    }
    return this.props.children;
  }
}
const s = StyleSheet.create({
  c: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 24, gap: 8 },
  t: { fontSize: 20, fontWeight: '700' }, m: { color: '#666', textAlign: 'center' },
  b: { marginTop: 12, backgroundColor: '#1a73e8', padding: 12, borderRadius: 10 },
  bt: { color: 'white', fontWeight: '600' },
});
```

- [ ] **Step 2: Envolver en el layout**

En `app/_layout.tsx`, importar y envolver: `return <ErrorBoundary><Slot /></ErrorBoundary>;`

- [ ] **Step 3: Typecheck + arranque**

Run: `npx tsc --noEmit` y abrir la app. Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: ErrorBoundary global"
```

---

## Task 16: Configuración de app + EAS + política de privacidad

**Files:**
- Modify: `app.json` (nombre, slug, android.package, icon, splash)
- Create: `eas.json`, `docs/privacidad.md`

**Interfaces:** ninguno (config y publicación).

- [ ] **Step 1: Configurar `app.json`**

Asegurar:
```json
{
  "expo": {
    "name": "HugoBox",
    "slug": "hugobox",
    "scheme": "hugobox",
    "android": { "package": "com.hugo.hugobox" },
    "plugins": ["expo-router", "@react-native-google-signin/google-signin"]
  }
}
```
(Ajustar `icon` y `splash` con assets reales cuando estén.)

- [ ] **Step 2: Crear `eas.json`**

```json
{
  "cli": { "version": ">= 5.0.0" },
  "build": {
    "development": { "developmentClient": true, "distribution": "internal" },
    "preview": { "distribution": "internal", "android": { "buildType": "apk" } },
    "production": { "android": { "buildType": "app-bundle" } }
  },
  "submit": { "production": {} }
}
```

- [ ] **Step 3: Política de privacidad**

`docs/privacidad.md`: redactar política simple (qué datos se guardan: correo y movimientos del usuario; dónde: Firebase/Google; para qué: funcionamiento de la app; contacto). Publicarla en una URL (GitHub Pages o similar) para enlazarla en Play Console.

- [ ] **Step 4: Build de prueba**

```bash
npm i -g eas-cli && eas login
eas build -p android --profile preview
```
Expected: genera un APK instalable para probar en un dispositivo real.

- [ ] **Step 5: Commit**

```bash
git add app.json eas.json docs/privacidad.md
git commit -m "chore: config de app HugoBox + EAS + política de privacidad"
```

---

## Task 17: Publicación en Play Store (checklist guiado)

**Files:** ninguno (proceso en Play Console). Marcar cada paso al completarlo.

- [ ] **Step 1:** Crear cuenta en Google Play Console (USD $25, pago único).
- [ ] **Step 2:** Crear la app en Play Console (nombre HugoBox, idioma, categoría Finanzas).
- [ ] **Step 3:** Generar AAB de producción: `eas build -p android --profile production`.
- [ ] **Step 4:** Configurar firma (Play App Signing) — EAS gestiona la keystore.
- [ ] **Step 5:** Completar ficha: descripción, capturas (mín. 2 de teléfono), ícono 512px, gráfico destacado, enlace a política de privacidad.
- [ ] **Step 6:** Completar cuestionarios: clasificación de contenido, seguridad de datos, público objetivo.
- [ ] **Step 7:** Subir el AAB con `eas submit -p android --profile production` (o manual).
- [ ] **Step 8:** Crear pista de **prueba cerrada** y reclutar **~12 testers**; mantener **14 días** (requisito para cuentas personales nuevas).
- [ ] **Step 9:** Tras el periodo de prueba, promover a **producción** y enviar a revisión.

---

## Self-Review (cobertura del spec)

- §2 Stack → Tasks 1, 5, 9, 10.
- §3 Arquitectura por capas / carpetas → Tasks 6–14 (servicios → hooks → UI).
- §4 Modelo de datos → Tasks 2, 6, 7.
- §5 Lógica (reparto, redondeo, egreso, reversión) → Tasks 3, 7 (TDD).
- §6 Auth + Security Rules → Tasks 8, 9.
- §7 Errores y offline → Tasks 5 (offline via Firestore), 12 (alerta egreso), 15 (boundary).
- §8 Testing → Tasks 1, 3, 4, 6, 7, 8, 12.
- §9 MVP (login, dashboard, ingreso/egreso, cajas, historial+filtros) → Tasks 9–14.
- §10 Play Store → Tasks 16, 17.

Sin placeholders vagos; nombres de funciones consistentes entre tareas (`crearCajasPorDefecto`, `agregarIngreso`, `agregarEgreso`, `borrarTransaccion`, `repartirIngreso`, `refCajas`, `refTransacciones`).
